/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["placeholder - has to be hidden(can send to you for testing)"], // Add your CloudFront distribution hostname here
      },

};

export default nextConfig;
